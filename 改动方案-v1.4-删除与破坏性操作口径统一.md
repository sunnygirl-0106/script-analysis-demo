# Script Analysis Demo · 改动方案 v1.4 — 删除与破坏性操作口径统一

> 面向 `script-analysis-demo` 仓库的执行文档。产品口径以本文为准，按本文实施即可，**不需要再做产品判断**。
> 遇到本文没写到的细节，按仓库既有风格处理，不要自行扩大范围。
> 本文承接 `改动方案-v1.3.md`，只改「删除 / 替换 / 重拆」这一族破坏性操作的**确认与撤销口径**，不动其它主线。

---

## 0. 本次要解决的问题

仓库里「删除 / 替换 / 重新拆分」几处的**二次确认、可撤销性、不可逆文案**各写各的，口径不一致；镜头删除原先有的「撤销」能力在一次改动里被误删，需要恢复。本次把这一族操作收敛到**同一套口径**，让用户在任何一处看到的信号都一致。

### 0.1 统一口径总表（本次的唯一事实源）

| 操作 | 触发前置 | 二次确认 | 可否撤销 | 撤销方式 | 落库时机 |
|---|---|---|---|---|---|
| **删除镜头** | 行内删除键 | 弹确认，文案写明**可撤销** | ✅ **可撤销** | 删除后 toast 内「撤销」按钮（5.2s 窗口） | 立即 |
| **删除场** | 场级 ⋯ 菜单 | **无镜头 → 直接删**；有镜头 → 弹确认，文案写明不可撤销 | ❌ 不可撤销 | — | 立即 |
| **删除集** | 集级 ⋯ 菜单 | 弹确认，文案写明不可撤销 | ❌ 不可撤销 | — | 立即 |
| **替换整个剧本** | 工具栏「导入新剧本」 | 选模式 → **模拟上传/粘贴** → 确认页（计数 + 勾选「我已了解」才可执行） | ❌ 不可撤销 | — | 确认后立即 |
| **替换本集剧本** | 集级 ⋯ 菜单 | 上传/粘贴 → 确认页（当前场/镜 vs 新内容识别）→ 拆解 | ❌ 不可撤销 | — | **拆解成功后才落库；失败原集不变** |
| **重新拆分本场 / 本集** | 场级 / 集级 ⋯ 菜单 | **配置弹窗本身即二次确认，不叠加额外确认** | ❌ 不可撤销 | — | 立即 |

**贯穿全表的一条原则**：只有「删除镜头」这一种单行、轻量、无级联的操作可撤销；凡是会级联（删场/删集连带清理独有资产 + 全树重编号）或整体替换（替换/重拆）的操作，一律**不可撤销**，改为用「硬确认」把关。因此「撤销」这个词**只**出现在删除镜头一处——用户由此学到一个稳定信号：**看到「撤销」＝这一步能反悔，看不到＝反悔不了，请在确认框里看清楚再点。**

### 0.2 不可逆文案统一为两句

全仓凡表达不可逆的地方，只用这两句，不再各写各的（现状散落着「无法撤销 / 无法恢复 / 无法保留」）：

- 不可撤销的操作：**「此操作不可撤销。」**
- 可撤销的删除镜头：**「此操作可撤销：删除后可在底部提示条中点『撤销』恢复。」**

### 0.3 现状盘点（重要：大部分替换 / 重拆已达标，不要重写）

v1.3 已经把替换/重拆的主体结构做好了。**先读代码确认现状，只做下面标注的增量，不要推倒重来**：

| 流程 | 现状 | 本次只需 |
|---|---|---|
| 替换本集 `ReplaceEpisodeDialog.tsx` | ✅ 已是独立三态机；确认页已对比「当前 N 场 N 镜 → 新内容 N 场 N 镜」；已「先拆解成功再落库、失败原集不变」；已标不可逆 | 仅把第 185 行「无法撤销」改为「不可撤销」（见 §6） |
| 替换整个剧本 `ScriptImportDialog.tsx` | ✅ 第 2 步已有确认页（集/场/镜/资产计数 + 「我已了解」勾选 + 勾了才可点） | 补一段**模拟上传/粘贴**；确认页文案对齐（见 §5） |
| 重拆本场 `ResplitSceneDialog.tsx` | ✅ 配置弹窗即确认，无叠加弹窗；已有「影响说明」 | 影响说明补「人工修改会被替换 + 不可撤销」（见 §7） |
| 重拆本集 `ResplitEpisodeDialog.tsx` | ✅ 同上 | 影响说明补「原有分镜/人工修改/镜头提示词会被替换 + 不可撤销」（见 §7） |
| 删除镜头 撤销 | ❌ **已被误删，本次恢复** | store + UI 都要改（见 §1、§4） |
| 删除场 空场直删 | ❌ 现在空场也弹确认 | 空场直删（见 §2） |

### 0.4 明确不做（Non-goals）

- 不接真实文件上传 / 真实拆解，全部走 demo 假数据（与 v1.3 一致）。
- **不做多级撤销栈**。删除镜头的撤销是「单级、随 toast 存活」，与既有 `renameAssetWithSync` 的撤销同款：新 toast 覆盖旧 toast，旧快照即失效。这是刻意的，够用且一致。
- **不给删除场 / 删除集 / 替换 / 重拆加撤销**。它们的口径就是「不可撤销 + 硬确认」，别顺手加。
- 不改 `services/` 里 R1–R14 已有规则的行为。

---

## 1. 数据/状态层 · 恢复「删除镜头」的撤销

**文件**：`src/store/useStore.ts` 的 `deleteShot`（现 501–532 行）

照抄仓库里现成的撤销范式——`renameAssetWithSync`（现 628–702 行）：在改动前拍快照，改动后 `showToast` 带一个 `{ label: '撤销', run }` 的动作。删除镜头要回滚三样：`project`、`promptStates`、`promptEdited`（删除时这三者都动了，见现 514–528 行）。

改成：

```ts
deleteShot: (shotId) => {
  if (!can(get().project, 'editShotFields')) return
  const st0 = get()
  const shot = st0.project.shots[shotId]
  if (!shot) return
  const scene0 = st0.project.scenes[shot.sceneId]
  if (!scene0) return
  const oldNo = scene0.shotIds.indexOf(shotId) + 1

  // 撤销快照：删除镜头会同时动 project / promptStates / promptEdited，三者一起回滚。
  // 单级、随 toast 存活，与 renameAssetWithSync 的撤销同款。
  const snapProject = st0.project
  const snapStates = st0.promptStates
  const snapEdited = st0.promptEdited

  set((s) => {
    const scene = s.project.scenes[shot.sceneId]
    if (!scene) return s
    const shotIds = scene.shotIds.filter((id) => id !== shotId)
    const shots = { ...s.project.shots }
    delete shots[shotId]
    shotIds.forEach((sid, i) => {
      const sh = shots[sid]
      if (sh && sh.no !== i + 1) shots[sid] = { ...sh, no: i + 1 }
    })
    const promptStates = { ...s.promptStates }
    delete promptStates[shotId]
    const promptEdited = { ...s.promptEdited }
    delete promptEdited[shotId]
    return {
      project: { ...s.project, scenes: { ...s.project.scenes, [shot.sceneId]: { ...scene, shotIds } }, shots },
      promptStates,
      promptEdited,
    }
  })

  get().showToast(`已删除第 ${oldNo} 镜`, {
    label: '撤销',
    run: () => set({ project: snapProject, promptStates: snapStates, promptEdited: snapEdited }),
  })
},
```

要点：
- 快照必须在 `set` 之前用 `st0`（旧引用）取；project 是不可变替换的，旧引用即完整前态。
- toast 文案不变（仍 `已删除第 N 镜`），只多挂 `action`。`Toast.tsx` 现有逻辑：带 `action` 的 toast 停留 5200ms（现 12 行），天然满足「5s 内可撤销」。**不用改 Toast.tsx。**
- 撤销只回滚上面三样即可：删除镜头不改 `selectedSceneId`，无需快照它。

---

## 2. 删除场 · 空场直删 / 有镜头才弹确认

**文件**：`src/components/EpisodeTree.tsx`

### 2.1 空场直接删除

场级菜单「删除本场」的 onClick（现 302–310 行）现在无条件打开确认弹窗。改为：**本场没有镜头就直接删，不弹窗**；有镜头才进确认弹窗。

```diff
  <button
    className={[s.menuItem, s.menuDanger].join(' ')}
    onClick={() => {
-     setDialog({ type: 'deleteScene', sceneId: sc.id })
-     setMenuScene(null)
+     setMenuScene(null)
+     // 空场无级联（没有镜头就不会有「仅在本场出现」的资产），直接删，不打扰。
+     if (sc.shotIds.length === 0) deleteScene(sc.id)
+     else setDialog({ type: 'deleteScene', sceneId: sc.id })
    }}
  >
    <i className={s.mIcon}>{ic.trash}</i>删除本场
  </button>
```

（`deleteScene` 已在现 64 行取到，直接用。）

### 2.2 有镜头的确认弹窗 · 文案对齐

确认弹窗（现 365–392 行）保留，`danger` 文案（现 369–375 行）按下述口径对齐。因为空场已在 §2.1 直删，这个弹窗只会在 `shots > 0` 时出现，无需再写「本场暂无镜头」分支：

```tsx
<div className={di.danger}>
  将同时删除本场的 {delScStat.shots} 个镜头
  {delScStat.onlyInScene > 0
    ? `，以及仅在本场出现的 ${delScStat.onlyInScene} 项独有资产。其他场仍在使用的内容会保留`
    : ''}
  。此操作不可撤销。
</div>
```

标题保持现状：`删除第 {delSc.no} 场「{delSc.name}」？`（现 368 行）。删除按钮、取消不动。**删除场不加撤销**（不可撤销是它的口径）。

---

## 3. 删除集 · 保持不可撤销，仅确认口径

**文件**：`src/components/EpisodeTree.tsx` 删除集确认（现 335–358 行）

现状文案（现 340 行）已符合口径，**保持不动**：

```
将同时删除本集的 N 场 N 个镜头。本集独有的 N 项角色、服装、场景和道具也会被删除；其他剧集仍在使用的内容会保留。此操作不可撤销。
```

删除集**不加撤销**。此节列出只为让「删镜可撤销 / 删场删集不可撤销」这条口径在文档里成组自明，代码无需改动（除非发现文案与上句有出入，则对齐）。

---

## 4. 删除镜头 UI · 二次确认文案改「可撤销」

**文件**：`src/components/ShotRow.tsx` 行内确认弹窗（现 468–491 行）

这里的删除交互（删除键 → 弹二次确认 → 折叠动画 → 调 `onDelete`）**结构不变**，`onDelete` 仍是父层 `Storyboard.tsx`（现 161 行）传下来的 `() => deleteShot(shot.id)`。只改确认文案（现 472–474 行 `di.danger`）：

```diff
  <div className={di.danger}>
-   本镜的镜头设计、出场的人和物以及已生成的提示词将一并移除，此操作不可撤销。
+   本镜的镜头设计、出场的人和物以及已生成的提示词将一并移除。此操作可撤销：删除后可在底部提示条中点「撤销」恢复。
  </div>
```

标题、取消/删除按钮、折叠动画（`removing` / `runDelete`）全部不动。撤销能力来自 §1 的 store 改动，UI 这里只需把文案从「不可撤销」翻正为「可撤销」。

---

## 5. 替换整个剧本 · 补模拟上传 + 确认页对齐

**文件**：`src/components/ScriptImportDialog.tsx`（+ 需要时 `.module.css`）

### 5.1 现状与目标

现状：`导入新剧本` 弹窗两个单选「追加到末尾 / 替换整个剧本」，选「替换整个剧本」后 `下一步` 直接进第 2 步确认页（已含计数 + 「我已了解」勾选，现 97–121 行）。缺的是：**替换整个剧本没有「上传/粘贴新内容」这一步**，直接用写死的 `altScriptPayload`。本次比照 `ReplaceEpisodeDialog` 的做法补上模拟上传/粘贴，让演示能走「提供新内容 → 确认 → 替换」的完整动线。

### 5.2 状态机改为三步

```diff
- const [step, setStep] = useState<1 | 2>(1)
+ type Step = 'mode' | 'upload' | 'confirm'
+ const [step, setStep] = useState<Step>('mode')
+ // 模拟上传：点上传区不打开系统文件选择器，直接置一个固定文件名；内容恒为 altScriptPayload。
+ const [srcTab, setSrcTab] = useState<'upload' | 'paste'>('upload')
+ const [fileName, setFileName] = useState<string | null>(null)
+ const [pasted, setPasted] = useState('')
```

动线：
- `mode` 步：两个单选不变。`追加到末尾` → `下一步` 仍直接 `appendEpisode2()` 后关闭（现 45–52 行 `onNext` 的 append 分支不变）。`替换整个剧本` → `下一步` 进 **`upload`** 步（原来是直接进确认）。
- `upload` 步：**新增**，比照 `ReplaceEpisodeDialog` 的 `input` 步（现 94–144 行）——两个 tab「上传文件 / 粘贴文本」：
  - 上传 tab：一个点击区，未选时显示「拖拽剧本文件到此处，或点击选择文件 / 支持 .docx / .txt / .md」，点击后模拟选中 `新剧本-完整版.docx`（`setFileName('新剧本-完整版.docx')`）。
  - 粘贴 tab：一个 `textarea`，placeholder「粘贴新的剧本内容…」。
  - 底部灰字：`上传或粘贴的内容将替换当前整个剧本；如包含多集，会一并替换。`
  - `取消` / `下一步`；未提供内容（上传无 fileName 且粘贴为空）时 `下一步` disabled。`下一步` → `confirm`。
  - 头注写明这是 demo 假数据：不打开系统选择器，内容恒为 `altScriptPayload`。
  - CSS 复用：可 `import ReplaceEpisodeDialog.module.css` 里的 `tabs/tab/tabOn/drop/dropOn/dropName/dropHint/paste/foot`，或把等价类补进 `ScriptImportDialog.module.css`。**优先直接 import 复用，别复制一套样式。**
- `confirm` 步：即原第 2 步（现 97–121 行）**保留**，`返回` 改为回到 `upload`（`setStep('upload')`，原来是回 step 1）。确认按钮仍 `disabled={!ack}`，点了调 `onConfirmOverwrite()`（现 54–57 行，仍 `replaceScript(altScriptPayload)`）。

### 5.3 确认页文案对齐

第 2 步 `danger`（现 100–102 行）改为统一口径：

```diff
- ⚠️ 新剧本《{altScriptPayload.title}》将替换当前剧本及全部分析内容。你此前的修改在替换后将无法恢复。
+ ⚠️ 新剧本《{altScriptPayload.title}》将替换当前剧本及全部分析结果。此操作不可撤销。
```

「我已了解」勾选行（现 103–108 行）保留：`我已了解：当前 N 集 / N 场 / N 镜头 / N 项相关素材 将被替换`。按钮文案 `替换剧本` 保留。

### 5.4 `mode` 步「替换整个剧本」选项描述对齐（现 82–84 行）

```diff
- 当前的剧本分析内容将被替换。你修改过的镜头时长、提示词以及出场人物和物品将无法保留。
+ 当前的剧本分析内容将被整个替换。下一步可上传或粘贴新剧本；替换后不可撤销。
```

### 5.5 打开时复位

`useEffect`（现 28–34 行）复位补上新状态：`setStep('mode')`、`setSrcTab('upload')`、`setFileName(null)`、`setPasted('')`、`setAck(false)`。

---

## 6. 替换本集 · 仅文案对齐（结构已达标）

**文件**：`src/components/ReplaceEpisodeDialog.tsx`

该组件已满足全部口径（独立流程、确认页对比当前/新内容、先拆解成功再落库、失败原集不变、标注不可逆）。**唯一改动**：确认页 `danger`（现 185 行）措辞统一：

```diff
- 其他剧集不受影响，此操作无法撤销。
+ 其他剧集不受影响，此操作不可撤销。
```

其余（`input` / `multi` / `confirm` / `running` / `failed` 各步、`MOCK_PARSE_MS`、`demoHint` 失败模拟）全部保持不动。

---

## 7. 重新拆分本场 / 本集 · 补「不可撤销 + 人工修改会被替换」

配置弹窗**本身就是二次确认，不再叠加额外确认弹窗**（现状已如此，保持）。只把「影响说明」补全到口径。

### 7.1 重拆本场

**文件**：`src/components/ResplitSceneDialog.tsx` 影响说明（现 129–132 行）

```diff
  <div className={s.impact}>
    <div className={s.impactTitle}>重新拆分后</div>
-   重新拆分后，本场的镜头和提示词将重新生成。已有角色造型、场景和道具会保留，其他场景不会改变。手动修改过的本场分镜将被替换。
+   点「确认重新拆分」后，本场原有分镜、人工修改和镜头提示词将被新的拆解结果替换；已有角色、服装、场景和道具继续保留，其他场不受影响。此操作不可撤销。
  </div>
```

### 7.2 重拆本集

**文件**：`src/components/ResplitEpisodeDialog.tsx` 影响说明（现 90–93 行）

```diff
  <div className={s.impact}>
    <div className={s.impactTitle}>影响说明</div>
-   本集 {stats.scenes} 场 {stats.shots} 镜将全部重新生成；已识别的角色 / 服装 / 场景 / 道具继续保留；其他集不受影响。
+   点「确认重新拆分」后，本集 {stats.scenes} 场 {stats.shots} 镜的原有分镜、人工修改和镜头提示词将被新结果替换；已有角色 / 服装 / 场景 / 道具继续保留，其他集不受影响。此操作不可撤销。
  </div>
```

两个弹窗的 `确认重新拆分` 按钮、参数区、`取消` 均不动。**不要再在点确认后弹第二个确认框。**

---

## 8. 全量文案核对表（改完逐条 grep 自查）

| 文件 | 现（关键片段） | 改后 |
|---|---|---|
| `store/useStore.ts` `deleteShot` | toast 无 action | toast 带 `撤销` action（§1） |
| `components/ShotRow.tsx` ~473 | `…一并移除，此操作不可撤销。` | `…一并移除。此操作可撤销：删除后可在底部提示条中点「撤销」恢复。` |
| `components/EpisodeTree.tsx` ~369 | 场删除 `danger`（含空场分支） | 收敛为有镜头文案，`此操作不可撤销`（§2.2） |
| `components/EpisodeTree.tsx` ~340 | 集删除 `…此操作不可撤销。` | 不变 |
| `components/ScriptImportDialog.tsx` ~83 | `…将无法保留。` | `…替换后不可撤销。`（§5.4） |
| `components/ScriptImportDialog.tsx` ~101 | `…将无法恢复。` | `…此操作不可撤销。`（§5.3） |
| `components/ReplaceEpisodeDialog.tsx` ~185 | `…此操作无法撤销。` | `…此操作不可撤销。` |
| `components/ResplitSceneDialog.tsx` ~131 | 无不可逆句 | 补「人工修改会被替换 + 不可撤销」（§7.1） |
| `components/ResplitEpisodeDialog.tsx` ~92 | 无不可逆句 | 补「原有分镜/人工修改/镜头提示词会被替换 + 不可撤销」（§7.2） |

自查命令：

```bash
grep -rn "无法撤销\|无法恢复\|无法保留" src/    # 期望：0 处（全部统一为「不可撤销」/「可撤销」）
grep -rn "可撤销" src/                          # 期望：仅 ShotRow（可撤销）+ 各不可撤销文案
grep -rn "撤销'\|label: '撤销'" src/store/       # 期望：renameAssetWithSync + deleteShot 两处
```

---

## 9. 测试与版本标注

按 README「规则版本追溯」章执行。

### 9.1 新增/补充测试

`src/tests/rules.test.ts`（该文件已有 store 级用例，用 `useStore.setState/getState`，照此风格加）：

- **删除镜头可撤销**（`R15 · since v1.4`）：
  - `deleteShot(id)` 后该镜从 `project.shots` / 所在场 `shotIds` / `promptStates` / `promptEdited` 全部移除，本场镜号顺延重编。
  - 取当前 `toast.action`，调用 `run()`，断言 `project` / `promptStates` / `promptEdited` **逐一回到删除前**（含镜号、`shotIds` 顺序、被删镜的 promptEdited 若原为 true 也应恢复）。
- **删除场不可撤销**（同 R15）：`deleteScene(id)` 后 `toast.action` 为 `undefined`（不提供撤销）。
- **空场直删**属 UI 行为（EpisodeTree 里判断），store 的 `deleteScene` 逻辑不变，无需为它加 store 测试；如仓库有组件测试可补，否则并入 §10 手测。

### 9.2 版本标注

- `src/tests/rules.test.ts` 顶部 `RULES_VERSION` → **`v1.4`**（现为 `v1.3`，注释 `对应改动方案 v1.4`）。
- 新增用例行内标 `// v1.4`；未改的保持原值。
- `deleteShot` 上方补一行注释注明「撤销：单级、随 toast 存活，见改动方案 v1.4 §1」。

### 9.3 README

「能真点的交互」一节补/改：

```
· **删除镜头可撤销（二次确认 → 删除 → 5 秒内 toast 撤销）**
· **删除场/集不可撤销（空场直删，有内容硬确认）**
· **替换整个剧本（模拟上传/粘贴 → 计数确认「我已了解」→ 替换）**
```

---

## 10. 验收清单

改完逐条点一遍：

- [ ] 删除**空场**（0 镜）：点「删除本场」**不弹窗**，直接删掉，选中场就近重指。
- [ ] 删除**有镜头的场**：弹确认，文案为「将同时删除本场的 N 个镜头，以及仅在本场出现的 N 项独有资产。其他场仍在使用的内容会保留。此操作不可撤销。」（无独有资产时省中间半句）；**无「撤销」**。
- [ ] 删除**镜头**：弹确认，文案含「此操作可撤销」；删除后 toast 出现「撤销」按钮；点撤销 → 该镜、镜号、提示词状态、手动编辑标记**完全复原**；不点则约 5 秒后 toast 自动消失。
- [ ] 删除**集**：确认文案「…此操作不可撤销。」；**无「撤销」**；至少保留 1 集的守卫仍在。
- [ ] **替换整个剧本**：选「替换整个剧本」→ 进上传/粘贴页（两个 tab，点上传区模拟选中 `新剧本-完整版.docx`）→ 确认页显示「当前 N 集 / N 场 / N 镜头 / N 项素材」+「我已了解」勾选，勾了才能点「替换剧本」；文案「此操作不可撤销」；`返回`回到上传页。
- [ ] **替换本集**：确认页对比「当前 N 场 N 镜 → 新内容 N 场 N 镜」；「替换并重新拆解」→ 转圈拆解 → 成功 toast 带 reused/added/removed；点「模拟拆解失败」→ 红字「原第 N 集未发生变化」，且树上第 N 集确实没变；文案「此操作不可撤销」。
- [ ] **重拆本场/本集**：配置弹窗点「确认重新拆分」**不再弹第二个确认框**；影响说明含「人工修改会被替换」「已有角色/服装/场景/道具保留」「其他场/集不受影响」「此操作不可撤销」。
- [ ] `grep -rn "无法撤销\|无法恢复\|无法保留" src/` **0 处**。
- [ ] `npm test` 全绿、`npm run typecheck` 无错。

---

## 附：口径速记（贴在 PR 描述里）

> 破坏性操作只有两种反悔口径：**删除镜头能撤销（toast 里点），其余（删场/删集/替换/重拆）都不能撤销、只能在确认框里看清后再点。** 文案只用「此操作可撤销 / 此操作不可撤销」两句。
