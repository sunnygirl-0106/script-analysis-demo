# DESIGN BRIEF — PhanthyMovie 剧本分析（script-analysis-demo）

> 把这份文件整份贴给 Claude Design（每个新会话都贴一次）。它包含：产品背景、必须保留的东西、禁止清单、要求清单、参考手法、以及本次会话的任务。

## 0. 产品是什么

一个影视前期工具：上传剧本 → 自动分集校对 → 提取角色 / 服装 / 场景 / 道具四类资产 → 按场拆分镜并生成画面提示词。用户是导演、制片、分镜师，一天要在里面盯几百行分镜表格。**这是专业的生产工具，不是营销页。** 视觉目标：像 Linear / Frame.io / DaVinci Resolve 那样"安静、密集、可长时间使用"的深色专业界面。

## 0.5 工作方式（最重要，先读这条）

**这是一次「修缮」，不是「重做」。** 现有产品已经上线可用，信息架构、页面布局、每个区块的位置、文案、交互流程全部保留。你的工作是在现有页面之上做样式层的修正：换主色、收字号、改圆角、去发光、去装饰、合并重复提示、统一图标。

具体规则：
- 以我提供的**现有页面截图 / HTML 为底稿**，在它上面改，不要另起一张空白画板从零画。
- 每张画板的布局骨架（顶栏、左侧导航、主区的分栏、表格的列、弹窗的内容顺序）与截图**一一对应**，元素不增不减，位置不动；只有第 5 节列出的"已知问题"允许改动结构（比如删掉重复的步骤条、把三卡片改成列表）。
- 改动之外的所有东西保持原样。拿不准要不要动的，**不动**，并在"设计决定说明"里列出来问我。
- 每张画板输出两个版本并排：左边「现状」（原样复刻截图），右边「修缮后」，让我一眼能对照出你改了什么。
- 如果你认为某个现有元素应该删掉或重排，先在说明里提出理由，不要直接删。

## 1. 必须保留（逐项，不要重新发明）

- 暗色主题，纯中性灰阶（无蓝调），页底近黑 `#121212`。
- 单一品牌主色：**`#02C5C8`**（logo 色）。代码里现在写的 `#8fdcd2` 是错的，请以 `#02C5C8` 为准重建整条主色色阶（见第 3 节）。主色只用于主按钮、当前选中、链接、进度。
- 四类实体色的机制不变：角色 紫 `#a78bfa` / 服装 粉 `#f472b6` / 道具 黄 `#fbbf24` 保留；**场景色需要换掉**——现在的绿 `#34d399` 与品牌青太近，请改成一个与品牌色、与其余三色都能一眼区分的色相（蓝或橙都可以，你来定，给出理由）。实体色只用于"标识类别"（色点、原文高亮），不用于按钮或背景。
- 现有信息架构：顶栏（logo / 充值 / 积分 / 通知 / 头像）、左侧项目导航（项目名 + 规格 chip、当前模式卡、五个入口、底部收起按钮）、主区顶部三步骤条（整理剧本 → 确认资产清单 → 生成分镜脚本）、主区底部操作栏（左状态文字、右主按钮）。
- 各页的布局骨架：
  - 剧本整理页：标题「共 N 集」+ 副标题 → 每集一个可折叠块（集号 / 标题 / 状态 tag / 字数 / 更多菜单）→ 正文段落。
  - 资产清单页：左「全剧原文」（带实体高亮）+ 右「资产表」（四类 tab 带计数 / 搜索 / 排序 / 新增按钮 / 表格：名称、造型、提示词、状态、操作）。
  - 分镜脚本页：左「场次树」（全剧 / 集 / 场 + 镜数）+ 右「分镜表」（镜号·时长 / 景别 / 镜头设计 / 出场人物 / 主要内容 / 最终提示词，按场分组，每行有 −/+ 调时长、删除）+ 底部进度与「生成全部提示词」。
  - 空状态页：标题 + 一句说明 + 上传按钮。
  - 弹窗：上传剧本（上传 / 粘贴两个 tab + 拖拽区）、拆分节奏（三档选项）、确认类；抽屉：场景设置。
- 剧本原文里的实体高亮（按角色 / 服装 / 场景 / 道具着色）与表格里的实体 chip 机制。
- 所有文案、按钮名称、积分标注（✦40 这类改成图标 + 数字，但信息保留）。
- 所有交互：折叠、tab 切换、拖拽上传、行内编辑、时长加减、撤销。
- 现有 `theme.css` 的 token 命名与"RGB 通道派生"机制，在它基础上修改取值，不要换一套变量名。

## 2. 禁止（NEVER）

颜色
- 不要任何紫/蓝/靛蓝渐变，不要渐变按钮，不要渐变头像占位。
- 不要彩色发光（colored box-shadow / glow），不要 radial-gradient 光晕背景，不要点阵 / 网格装饰背景。
- 不要玻璃拟态（backdrop-blur）。

形状
- 按钮不要用 pill（999px）。pill 只允许用于 tag / chip。
- 容器圆角 ≤ 10px，控件 ≤ 6px。不要 16px+ 的圆角卡片。
- 不要卡片套卡片；不要"细描边 + 大阴影"同时用。
- 不要 hover 位移（translateY）、不要 hover 缩放；阴影只给菜单 / 弹窗 / 抽屉这类真正浮起的层。
- 不要卡片左侧彩色粗边；不要"圆角方块图标容器在标题上方"的 feature-card 模板；不要三张等宽卡片一排的选项布局（改成单选列表或分段控件）。

文字
- 字号只允许：11 / 12 / 13 / 15 / 18 / 24px。不要 9 / 9.5 / 10 / 10.5 / 11.5 / 12.5。
- 不要 letter-spacing 拉开的全大写小标签（如 "EPISODE 01"）。
- 不要 emoji 或 Unicode 符号当图标（✦ 🗑 🔔 ▶ ⋯ ＋ ✕ 全部换成同一套线性图标，Lucide 风格，1.5px 线宽）。
- 不要营销词（智能、极速、赋能、一站式）；按钮文案 = 动词 + 宾语。
- 不要破折号 ——。

信息与反馈
- 同一信息不出现第二次：不要重复的步骤条、不要"已选 X"总结栏、不要在页面和弹窗里都写"支持 txt / docx / fdx"。
- 不要每个控件下面都有一行灰色 helper text。
- 不要每个操作都弹 toast。toast 仅用于：后台任务完成、可撤销的删除。
- 不要"推荐 / NEW"徽章。
- 不要装饰动效：pulse 圆点、bounce / elastic 缓动、假进度表演、跑马灯。

## 3. 要求（ALWAYS）

- 层级靠字重 (400 / 500 / 600)、颜色 (t1 / t2 / t3) 和间距表达，不靠加框、加 chip、加背景。
- 间距节奏：组内 4–8，组间 16–24，区块间 32–48。
- 表面分层表达深度：bg0 页底 → bg1 面板 → bg2 抬起控件 → 浮层用阴影。平面元素用 1px 线，浮起元素用阴影，二选一。
- 密集表格：正文 13px，行高 1.5，纯文字优先；每格最多一行 chip；状态 = 4px 色点 + 文字。
- 主色系统：以 `#02C5C8` 为 500 档，派生 50–900 共 9 档色阶（保持色相 ≈ 181°，只动明度/饱和度），并给每档规定用途：
  - 400（约 `#33D3D5`）：深色底上的链接文字、图标高亮、当前步骤数字
  - 500 `#02C5C8`：主按钮实底、选中态描边、进度条、焦点环
  - 600 / 700（约 `#02A3A6` / `#017E80`）：主按钮 hover / active
  - 深色主题的淡底不用实色，一律用 `rgb(2 197 200 / 0.10)`（背景）和 `rgb(2 197 200 / 0.35)`（描边）
  - 主色上的文字用深墨 `#062A2B`，不用白字（对比度不够）
  - 全页主色出现次数 ≤ 5 处；主色不做发光、不做渐变、不做 chip 底、不做装饰
- 主按钮：`#02C5C8` 实底 + `#062A2B` 字，6px 圆角；次按钮：1px 线 + 透明底；危险按钮：只在确认步骤变红。
- 每个可交互元素有 hover（仅换底色）、focus-visible（2px 外描边）、active、disabled 四态。
- 每个页面设计 空 / 加载 / 错误 三态。
- 空状态：一行标题 + 一句说明 + 一个主按钮，不要插画、不要光晕、不要重复步骤图。
- 弹窗：按用途分三种壳——确认（窄，320–380px，标题 + 一句话 + 两个按钮）、表单（460px）、选择（列表式单选，不是卡片）。
- 批量操作栏：紧贴表格底部、通宽、直角或 4px 圆角、深色实底、左侧"已选 N 项"、右侧操作按钮，无小圆点、无彩色描边。
- 对比度：正文 ≥ 4.5:1，辅助 ≥ 3:1。
- 字体：中文 PingFang SC / 思源黑体；数字与时间码用 JetBrains Mono 或 Geist Mono（tabular-nums）。

## 4. 参考手法（借手法，不借外观）

- Linear：主色用量、灰阶层级、列表行密度、hover 只换底色。
- Frame.io / DaVinci Resolve：深色影视工具的表面分层与时间码排版。
- Notion 数据库视图：表格里少量 tag 的用法，其余纯文字。
- Raycast：暗色下菜单 / 浮层的阴影与描边处理。
- Figma 属性面板：密集控件的对齐与分组。

## 5. 现状里已知的问题（你要修的）

- 主色写错：`theme.css` 里 `--acc-rgb: 143 220 210`（#8fdcd2），应改为 `2 197 200`（#02C5C8），`--acc-hi`、`--on-acc` 需按新色阶重定。
- 44 处 pill 圆角，按钮与标签无差别。
- 主按钮 hover 上浮 2px + 主色发光；次按钮 hover 上浮 1px。
- 空状态页：radial 光晕 + 点阵背景 + 几何拼贴插画 + 顶部与底部两条步骤条。
- 12 种字号，200+ 处 ≤ 12.5px。
- 24 种 toast；toast 样式为底部居中圆角长条 + 主色描边 + 小圆点 + 撤销 pill。
- 拆分弹窗：三张等宽卡片 + 「推荐」徽章 + 下方"已选"总结栏。
- 分镜表格每格 3 行 chip，每 chip 带色点，扫读困难。
- emoji / Unicode 当图标混用。
- 侧栏头像为主色→紫渐变。
- 所有弹窗共用一个 460px 壳。

## 6. 本次会话的任务（每次只勾一个）

- [ ] 会话 A：**设计系统画板**。以现有 `theme.css` 为底，只改取值不改变量名：`#02C5C8` 派生的主色色阶与用途表、中性灰阶、四类实体色（含新的场景色）、字阶、间距、圆角、阴影；然后把现有的按钮 / chip / tab / 输入框 / 表格行 / 步骤条 / toast / 弹窗壳 / 抽屉壳 / 菜单逐个做「现状 → 修缮后」对照，每个组件给 default / hover / active / disabled 四态。产出后导出 `theme.css`、`ui.module.css`、`dialog.module.css`、`Toast.module.css` 的修订版（保留原有 class 名）。
- [ ] 会话 B：**三张页面的修缮稿**。在我提供的截图底稿上改：剧本整理、资产清单、分镜脚本，各出「现状 / 修缮后」并排，1440×900。布局骨架、元素、文案不动，只应用会话 A 的系统和第 5 节的修正项。分镜表按截图里的真实密度（8 镜一场、每格 3 类实体）出。
- [ ] 会话 C：**覆盖层修缮稿**。上传剧本弹窗、拆分节奏弹窗、删除确认、场景设置抽屉、toast、批量操作栏，同样「现状 / 修缮后」并排。
- [ ] 会话 D（可选）：空状态、全页处理中、错误态。

输出要求：每张画板附一段 ≤ 10 行的「设计决定说明」：改了什么、为什么、以及哪些你想改但没动、需要我拍板的。
