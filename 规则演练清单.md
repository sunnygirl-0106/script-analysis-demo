# 规则演练清单

> 本文件由 `npm run drills:md` 从 `src/services/drills.ts` 的 `DRILLS` 自动生成，请勿手改。
> 每条演练同时是 `tests/drills.test.ts` 的一条数据驱动断言。

| 分组 | 操作 | 查不查资产 | 资产库变化 | 分镜变化 | 是否停下等确认 |
| --- | --- | --- | --- | --- | --- |
| ② 剧本原文变了 → 对应范围重建分镜 | 追加剧集 | 新增集原文 | 只增 | 只生成新集，旧集不动 | 有新候选才停 |
| ② 剧本原文变了 → 对应范围重建分镜 | 替换本集 | 新集原文 | 只增（旧集独有资产变未引用，不删） | 仅换本集 | 有新候选才停 |
| ② 剧本原文变了 → 对应范围重建分镜 | 重拆本场（有漏提） | 本场原文 | 只增 | 仅本场 | 有漏提才停 |
| ② 剧本原文变了 → 对应范围重建分镜 | 重拆本场（无漏提） | 本场原文 | 不变 | 仅本场 | 零候选不打断 |
| ② 剧本原文变了 → 对应范围重建分镜 | 重拆本集 | 本集原文 | 只增 | 本集各场 | 任一场有漏提即停 |
| 删除操作 → 资产库只增不减 | 删场 | 删一个场 | 一条不减 | 级联删镜 | 不开闸 |
| 删除操作 → 资产库只增不减 | 删集 | 删一个集 | 一条不减（本集独有资产变未引用） | 级联删场镜 | 不开闸 |
| 删除操作 → 资产库只增不减 | 删镜 | 删一个镜 | 一条不减 | 顺延重编号 | 不开闸 |
| 删除操作 → 资产库只增不减 | 删资产（唯一减少出口） | 资产库删一项 | 减一 | 挂载失效但保留 | 不开闸 |
| 造型手动挂载 | 新建造型 | 给角色挂造型 | 只增 | 不动 | 不开闸 |
| 造型手动挂载 | 换服装 | 改造型引用的服装 | 不变 | 引用镜头标待更新 | 不开闸 |
| 单向传播 | 改资产提示词 | 改一版提示词 | 不变 | 引用镜头标待更新 | 不开闸 |
| ② 剧本原文变了 → 对应范围重建分镜 | 插入空白场 | 不带原文的新场 | 不变 | 空场无镜 | 空白场不走闸 |

## 附：v3 六条口径原文

**R1 · 先确认资产，再生成分镜**
剧本导入后的处理分为两个阶段：① AI 提取角色、服装、场景、道具，用户确认资产清单；② 资产确认并存入项目资产库后，再生成集、场、镜及分镜脚本。最终画面提示词和视频提示词属于下一阶段，不与分镜脚本同时生成。

**R2 · 项目资产库只增不减**
首次导入、追加剧集、新增场、替换集/场文本、重新拆解本集、重新拆解本场，只能向项目资产库新增资产，不自动覆盖或删除已有资产。

**R3 · 已有资产及图片受保护**
用户手动创建、修改、上传图片或生成图片的资产，不会被剧本重新分析自动覆盖。删除项目资产库中的资产只有一个入口：用户在项目资产库中主动删除。

**R4 · 同名资产按已有资产处理**
本次候选名称已存在于项目资产库时，不重复新增。若用户认为是同名的不同资产，由用户修改名称后再作为新资产保存。

**R5 · 资产确认后再写库**
AI 提取完成时，新增候选暂不写入项目资产库。用户确认后，系统才执行增量保存。

**R6 · 已入库资产回到项目资产库管理**
资产清单确认阶段用于检查本次提取结果。已入库资产的后续新增、删除、改名、提示词修改、图片上传和图片生成，统一在项目资产库中完成。
